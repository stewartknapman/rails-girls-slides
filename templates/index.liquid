<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  
  <title>{{ title }}</title>
  
  <link rel="stylesheet" href="/styles.css">
  <script src="/app.js" async></script>
  <script src="https://use.typekit.net/ypv0yzb.js"></script>
  <script>try{Typekit.load({ async: true });}catch(e){}</script>
</head>
<body>
  
  <div class="slider">
    <ol class="slides" data-slide-count="{{ slides.length }}">
      {% for slide in slides %}
        <li id="{{ slide.title | handle }}" data-slide>
          <div class="slide-content {{ slide.class }}">
            {{ slide.content }}
          </div>
          <div class="slide-notes">
            <div class="slide-count">{{ forloop.index }}/{{ slides.length }}</div>
            {{ slide.notes }}
          </div>
        </li>
      {% endfor %}
    </ol>
    
    <ul class="slider-controls">
      <li><a href="#" data-slider-control="prev"><span class="control-text">Previous</span></a></li>
      <li><a href="#" data-slider-control="next"><span class="control-text">Next</span></a></li>
    </ul>
  </div>
  
</body>
</html>